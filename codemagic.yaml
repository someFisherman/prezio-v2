workflows:
  # ==========================================
  # Android APK Build (ohne Signierung)
  # ==========================================
  android-build:
    name: Android Build
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      java: 17
    scripts:
      - name: Get Flutter packages
        script: flutter pub get
      
      - name: Build APK (Debug)
        script: flutter build apk --debug
      
      - name: Build APK (Release - unsigned)
        script: flutter build apk --release
    
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
    
    publishing:
      email:
        recipients:
          - gloognoe@gmail.com
        notify:
          success: true
          failure: true

  # ==========================================
  # iOS Build (signiert - Apple Developer)
  # ==========================================
  ios-build-signed:
    name: iOS Build (Signed)
    max_build_duration: 30
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Prezio
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      ios_signing:
        distribution_type: app_store
        bundle_identifier: ch.soleco.prezioV2
    scripts:
      - name: Get Flutter packages
        script: flutter pub get
      
      - name: Install CocoaPods dependencies
        script: |
          cd ios
          pod install
      
      - name: Set up code signing settings on Xcode project
        script: xcode-project use-profiles
      
      - name: Build iOS IPA (signed)
        script: |
          flutter build ipa --release \
            --export-options-plist=/Users/builder/export_options.plist
      
      - name: Verify IPA exists
        script: |
          echo "=== build/ios/ipa/ ==="
          ls -la build/ios/ipa/ 2>/dev/null || echo "build/ios/ipa/ existiert nicht"
          IPA_COUNT=$(ls build/ios/ipa/*.ipa 2>/dev/null | wc -l)
          echo "Gefundene IPA-Dateien: $IPA_COUNT"
          if [ "$IPA_COUNT" -eq 0 ]; then
            echo "FEHLER: Keine IPA gefunden!"
            exit 1
          fi
    
    artifacts:
      - build/ios/ipa/*.ipa
    
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
      email:
        recipients:
          - gloognoe@gmail.com
        notify:
          success: true
          failure: true
  # ==========================================
  # iOS Build (unsigned - Fallback ohne Signierung)
  # Nutzen: Wenn ios-build-signed fehlschlägt, liefert dieser Workflow
  # eine IPA zum manuellen Testen. Oder zum Testen, ob Flutter/Xcode überhaupt baut.
  # ==========================================
  ios-build-unsigned:
    name: iOS Build (Unsigned)
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Get Flutter packages
        script: flutter pub get
      
      - name: Install CocoaPods dependencies
        script: |
          cd ios
          pod install
      
      - name: Build iOS (unsigned)
        script: |
          export CODE_SIGN_IDENTITY=""
          export CODE_SIGNING_REQUIRED=NO
          flutter build ios --release --no-codesign
      
      - name: Create unsigned IPA
        script: |
          cd build/ios/iphoneos
          mkdir -p Payload
          cp -r Runner.app Payload/
          zip -r prezio_unsigned.ipa Payload
          mv prezio_unsigned.ipa ../../.././
    
    artifacts:
      - "*.ipa"
      - build/ios/iphoneos/Runner.app
    
    publishing:
      email:
        recipients:
          - gloognoe@gmail.com
        notify:
          success: true
          failure: true

  # ==========================================
  # Beide Plattformen zusammen
  # ==========================================
  build-all:
    name: Build All (Android + iOS)
    max_build_duration: 45
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Prezio
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      java: 17
      ios_signing:
        distribution_type: app_store
        bundle_identifier: ch.soleco.prezioV2
    scripts:
      - name: Get Flutter packages
        script: flutter pub get
      
      # Android
      - name: Build Android APK
        script: flutter build apk --release
      
      # iOS
      - name: Install CocoaPods
        script: |
          cd ios
          pod install
      
      - name: Set up code signing settings on Xcode project
        script: xcode-project use-profiles
      
      - name: Build iOS IPA (signed)
        script: |
          flutter build ipa --release \
            --export-options-plist=/Users/builder/export_options.plist
    
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
      - build/ios/ipa/*.ipa
    
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
      email:
        recipients:
          - gloognoe@gmail.com
        notify:
          success: true
          failure: true
